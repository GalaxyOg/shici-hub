{
  "name": "shici-memory",
  "version": "1.1.0",
  "private": true,
  "description": "拾词：IELTS、TOEFL、GRE 与 PTE Academic 的 BS 间隔复习网页应用",
  "author": "落日七号",
  "repository": {
    "type": "git",
    "url": "https://github.com/luori7hao/shici-memory.git"
  },
  "homepage": "https://luori7hao.github.io/shici-memory/",
  "scripts": {
    "test": "npm run test:smoke && npm run test:sync && npm run test:api",
    "test:smoke": "node tests/smoke.js",
    "test:sync": "node tests/sync_test.js",
    "test:api": "python3 -m unittest tests.api_test -v",
    "check": "node --check app.js && node --check tests/smoke.js && node --check tests/sync_harness.js && node --check tests/sync_test.js && python3 -m py_compile server.py tests/api_test.py"
  }
}
